v1:
  provides:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
      redirectURIs:
        type: array
        items:
          type: string
      secret:
        type: string
    required:
      - id
      - name
      - redirectURIs
      - secret
